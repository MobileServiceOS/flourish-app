# The proxy, for any host that reads a Procfile (Fly, Heroku, Render).
# Railway takes its start command from railway.json; this keeps the repo
# portable so the choice of host is not baked into the code.
web: node server/index.js

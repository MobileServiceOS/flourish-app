node_modules/
dist/
.DS_Store
*.log

# Native platform folders — regenerate with `npx cap add ios|android`
ios/
android/

# Never commit these
.env
.env.local
# Vite reads these too, and they hold the app key and the proxy URL.
.env.*.local
.env.production
clover-secrets.json
# Editor backups of the above — nano/vim leave these behind holding live tokens
*.save
*.swp
.env.*.bak

# Simulator captures — regenerate, do not version
brand/screenshots/

# gstack skill reports and screenshots
.gstack/
screenshots/

# iOS build output for the screenshot script
.screenshot-build/
